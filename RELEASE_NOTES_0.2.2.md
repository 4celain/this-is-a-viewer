# This is a Viewer 0.2.2

Version: `0.2.2 (13)`

This update makes frequently used folders and multiple archives faster and
more predictable to browse.

## What’s New

- Adds a collapsible folder navigator to the left of the existing thumbnail
  sidebar.
- Lets you add folders, ZIP files and CBZ files to Favorites and reopen recent
  locations.
- Loads subfolders and archives only when a favorite is expanded.
- Lets you reorder favorites by dragging and undo adding, removing, reordering
  or clearing favorites with `Command-Z` or the inline Undo button.
- Adds a folder, ZIP or CBZ to Favorites only when it is dropped on the folder
  navigator. Dropping it on the viewer or thumbnail sidebar opens it directly.
- Lets you hide Recent from the navigator and show it again in Settings.
- Lets you pin a recent location to Favorites or remove a favorite.
- Includes ZIP and CBZ files immediately inside the selected folder by default.
  A setting can also include images and archives in all descendant folders.
- Sorts top-level folders, ZIP files and CBZ files naturally when several are
  opened together, then keeps each container’s images together.
- Remembers the selected video mute state while the app remains open. Every new
  app launch still starts muted.
- Prevents Delete from removing a real folder or the currently viewed media
  while keyboard focus is in the folder navigator.

## System Requirements

- macOS 14 or later
- Apple Silicon Mac
- Direct and Mac App Store editions

RAR, 7z and CBR are not included in this release. ZIP and CBZ files appear as
single items in the folder tree; folders inside an archive are not expanded in
the tree.

---

## 한국어

0.2.2는 반복해서 여는 폴더와 여러 압축파일을 더 빠르고 예측 가능하게
탐색하는 업데이트입니다.

### 변경 사항

- 기존 썸네일 사이드바 왼쪽에 접을 수 있는 폴더 탐색 패널을 추가했습니다.
- 폴더·ZIP·CBZ를 즐겨찾기에 추가하고 최근에 연 위치를 다시 열 수 있습니다.
- 즐겨찾기 폴더는 펼칠 때만 하위 폴더와 압축파일을 읽습니다.
- 즐겨찾기 순서를 드래그로 바꾸고 추가·제거·순서 변경·전체 제거를
  `Command-Z` 또는 인라인 복귀 버튼으로 되돌릴 수 있습니다.
- 폴더 탐색 영역에 놓은 폴더·ZIP·CBZ만 즐겨찾기에 등록하며, 본문이나
  썸네일 사이드바에 놓으면 바로 열기만 합니다.
- 최근 항목 섹션은 패널에서 바로 가리거나 설정에서 다시 표시할 수 있습니다.
- 최근 항목을 즐겨찾기에 고정하고 즐겨찾기에서 제거할 수 있습니다.
- 선택한 폴더 바로 아래 ZIP·CBZ는 기본 목록에 포함하고, 설정에서 하위
  폴더의 이미지와 압축파일까지 포함하는 재귀 탐색을 선택할 수 있습니다.
- 여러 폴더·ZIP·CBZ를 함께 열면 최상위 컨테이너를 자연 정렬하고, 각
  컨테이너의 이미지가 서로 섞이지 않게 연속해서 표시합니다.
- 영상 음소거를 해제하거나 다시 켜면 앱을 종료하기 전까지 선택한 상태를
  유지합니다. 앱을 새로 실행할 때는 기본 음소거로 시작합니다.
- 폴더 탐색기에 키보드 포커스가 있을 때 삭제 단축키로 실제 폴더나 현재
  보고 있던 미디어가 잘못 삭제되지 않도록 차단합니다.

### 지원 환경

- macOS 14 이상
- Apple Silicon Mac
- Direct판과 Mac App Store판

RAR·7z·CBR는 이번 버전에 포함하지 않습니다. ZIP·CBZ는 폴더 트리에서
하나의 파일 항목으로 열며, 압축파일 내부 폴더를 트리로 펼치지는 않습니다.
